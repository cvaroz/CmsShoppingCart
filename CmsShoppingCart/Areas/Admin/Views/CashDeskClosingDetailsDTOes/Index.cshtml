@model IEnumerable<CmsShoppingCart.Areas.Admin.Models.CashDeskClosingDetailsDTO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CashDesksClosing.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Orders.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Users.FirstName)
        </th>
        <th>
           SubTotal
        </th>
        <th>
            Total
        </th>
      
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CashDesksClosing.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Orders.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Users.FirstName)
        </td>
        <td>
           
            @(item.Orders.Products.Price * item.Orders.Quantity)
        </td>
       
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
       
    </tr> 
  
}




</table>
<td>

<td class="grandtotal"><b>Grand total</b> $<span>@ViewBag.GrandTotal</span></td>
    </td>

